{
  "name": "angular2-ror-heroku-project",
  "version": "1.0.0",
  "description": "angular2 app for heroku on rails",
  "main": "no",
  "engines": {
    "node": "6.9.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd client && npm install && npm run build"
  }
}
